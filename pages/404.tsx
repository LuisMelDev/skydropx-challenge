import { Layout, NotFoundView } from "components";
import Link from "next/link";

const Custom404 = () => {
    return (
        <Layout>
            <NotFoundView />
        </Layout>
    );
};

export default Custom404;
